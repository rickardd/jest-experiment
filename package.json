{
  "name": "jest-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "jest --verbose",
    "test:watch": "jest --watchAll --verbose",
    "test:pattern": "jest --verbose -t",
    "test:watch:pattern": "jest --watchAll --verbose -t"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.9",
    "babel-jest": "^29.6.1",
    "jest": "^29.6.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
